# Sidecar scripts (Python, offline)

`generate-depth.py` lands in M1 (docs/ROADMAP.md): wraps DAP folder inference
and writes `<scene>.npz` + provenance JSON per pano. See docs/MODEL-DAP.md
for the DAP install and the output contract.
